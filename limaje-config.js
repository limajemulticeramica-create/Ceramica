/* Edite con su Project URL y anon public (Supabase → Settings → API).
   Si el repositorio en GitHub es público, no suba este archivo con claves reales:
   use un repo privado o cambie las claves solo en Netlify (editor de archivos del deploy). */
window.LIMAJE_SUPABASE_URL = window.LIMAJE_SUPABASE_URL || '';
window.LIMAJE_SUPABASE_ANON_KEY = window.LIMAJE_SUPABASE_ANON_KEY || '';
